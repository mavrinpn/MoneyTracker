// coverage:ignore-file
// GENERATED CODE - DO NOT MODIFY BY HAND
// ignore_for_file: type=lint, unused_element

part of 'color_theme_extension.dart';

// **************************************************************************
// ThemeExtensionsGenerator
// **************************************************************************

mixin _$ThemeExtensionMixin on ThemeExtension<ColorThemeExtension> {
  @override
  ThemeExtension<ColorThemeExtension> copyWith({
    ThemeMode? themeMode,
    Color? accentColor,
    Color? scaffoldBackgroundColor,
    Color? headerTitleColor,
    Color? headerBackgroundColor,
    Color? bottomBackgroundColor,
    Color? selectedItemColor,
    Color? unselectedItemColor,
    Color? subviewBackgroundColor,
    Color? primaryButtonColor,
    Color? secondaryButtonColor,
    Color? successButtonColor,
    Color? warningButtonColor,
    Color? dangerButtonColor,
    Color? buttonLabelColor,
    Color? placeholderColor,
    Color? titleTextColor,
    Color? subtitleTextColor,
    Color? tileBackgroundColor,
    Color? avatarBackgroundColor,
  }) {
    final object = this as ColorThemeExtension;

    return ColorThemeExtension(
      themeMode: themeMode ?? object.themeMode,
      accentColor: accentColor ?? object.accentColor,
      scaffoldBackgroundColor:
          scaffoldBackgroundColor ?? object.scaffoldBackgroundColor,
      headerTitleColor: headerTitleColor ?? object.headerTitleColor,
      headerBackgroundColor:
          headerBackgroundColor ?? object.headerBackgroundColor,
      bottomBackgroundColor:
          bottomBackgroundColor ?? object.bottomBackgroundColor,
      selectedItemColor: selectedItemColor ?? object.selectedItemColor,
      unselectedItemColor: unselectedItemColor ?? object.unselectedItemColor,
      subviewBackgroundColor:
          subviewBackgroundColor ?? object.subviewBackgroundColor,
      primaryButtonColor: primaryButtonColor ?? object.primaryButtonColor,
      secondaryButtonColor: secondaryButtonColor ?? object.secondaryButtonColor,
      successButtonColor: successButtonColor ?? object.successButtonColor,
      warningButtonColor: warningButtonColor ?? object.warningButtonColor,
      dangerButtonColor: dangerButtonColor ?? object.dangerButtonColor,
      buttonLabelColor: buttonLabelColor ?? object.buttonLabelColor,
      placeholderColor: placeholderColor ?? object.placeholderColor,
      titleTextColor: titleTextColor ?? object.titleTextColor,
      subtitleTextColor: subtitleTextColor ?? object.subtitleTextColor,
      tileBackgroundColor: tileBackgroundColor ?? object.tileBackgroundColor,
      avatarBackgroundColor:
          avatarBackgroundColor ?? object.avatarBackgroundColor,
    );
  }

  @override
  ThemeExtension<ColorThemeExtension> lerp(
    ThemeExtension<ColorThemeExtension>? other,
    double t,
  ) {
    final otherValue = other;

    if (otherValue is! ColorThemeExtension) {
      return this;
    }

    final value = this as ColorThemeExtension;

    return ColorThemeExtension(
      themeMode: otherValue.themeMode,
      accentColor: Color.lerp(
        value.accentColor,
        otherValue.accentColor,
        t,
      )!,
      scaffoldBackgroundColor: Color.lerp(
        value.scaffoldBackgroundColor,
        otherValue.scaffoldBackgroundColor,
        t,
      )!,
      headerTitleColor: Color.lerp(
        value.headerTitleColor,
        otherValue.headerTitleColor,
        t,
      )!,
      headerBackgroundColor: Color.lerp(
        value.headerBackgroundColor,
        otherValue.headerBackgroundColor,
        t,
      )!,
      bottomBackgroundColor: Color.lerp(
        value.bottomBackgroundColor,
        otherValue.bottomBackgroundColor,
        t,
      )!,
      selectedItemColor: Color.lerp(
        value.selectedItemColor,
        otherValue.selectedItemColor,
        t,
      )!,
      unselectedItemColor: Color.lerp(
        value.unselectedItemColor,
        otherValue.unselectedItemColor,
        t,
      )!,
      subviewBackgroundColor: Color.lerp(
        value.subviewBackgroundColor,
        otherValue.subviewBackgroundColor,
        t,
      )!,
      primaryButtonColor: Color.lerp(
        value.primaryButtonColor,
        otherValue.primaryButtonColor,
        t,
      )!,
      secondaryButtonColor: Color.lerp(
        value.secondaryButtonColor,
        otherValue.secondaryButtonColor,
        t,
      )!,
      successButtonColor: Color.lerp(
        value.successButtonColor,
        otherValue.successButtonColor,
        t,
      )!,
      warningButtonColor: Color.lerp(
        value.warningButtonColor,
        otherValue.warningButtonColor,
        t,
      )!,
      dangerButtonColor: Color.lerp(
        value.dangerButtonColor,
        otherValue.dangerButtonColor,
        t,
      )!,
      buttonLabelColor: Color.lerp(
        value.buttonLabelColor,
        otherValue.buttonLabelColor,
        t,
      )!,
      placeholderColor: Color.lerp(
        value.placeholderColor,
        otherValue.placeholderColor,
        t,
      )!,
      titleTextColor: Color.lerp(
        value.titleTextColor,
        otherValue.titleTextColor,
        t,
      )!,
      subtitleTextColor: Color.lerp(
        value.subtitleTextColor,
        otherValue.subtitleTextColor,
        t,
      )!,
      tileBackgroundColor: Color.lerp(
        value.tileBackgroundColor,
        otherValue.tileBackgroundColor,
        t,
      )!,
      avatarBackgroundColor: Color.lerp(
        value.avatarBackgroundColor,
        otherValue.avatarBackgroundColor,
        t,
      )!,
    );
  }

  @override
  bool operator ==(Object other) {
    final value = this as ColorThemeExtension;

    return identical(this, other) ||
        (other.runtimeType == runtimeType &&
            other is ColorThemeExtension &&
            identical(value.themeMode, other.themeMode) &&
            identical(value.accentColor, other.accentColor) &&
            identical(
                value.scaffoldBackgroundColor, other.scaffoldBackgroundColor) &&
            identical(value.headerTitleColor, other.headerTitleColor) &&
            identical(
                value.headerBackgroundColor, other.headerBackgroundColor) &&
            identical(
                value.bottomBackgroundColor, other.bottomBackgroundColor) &&
            identical(value.selectedItemColor, other.selectedItemColor) &&
            identical(value.unselectedItemColor, other.unselectedItemColor) &&
            identical(
                value.subviewBackgroundColor, other.subviewBackgroundColor) &&
            identical(value.primaryButtonColor, other.primaryButtonColor) &&
            identical(value.secondaryButtonColor, other.secondaryButtonColor) &&
            identical(value.successButtonColor, other.successButtonColor) &&
            identical(value.warningButtonColor, other.warningButtonColor) &&
            identical(value.dangerButtonColor, other.dangerButtonColor) &&
            identical(value.buttonLabelColor, other.buttonLabelColor) &&
            identical(value.placeholderColor, other.placeholderColor) &&
            identical(value.titleTextColor, other.titleTextColor) &&
            identical(value.subtitleTextColor, other.subtitleTextColor) &&
            identical(value.tileBackgroundColor, other.tileBackgroundColor) &&
            identical(
                value.avatarBackgroundColor, other.avatarBackgroundColor));
  }

  @override
  int get hashCode {
    final value = this as ColorThemeExtension;

    return Object.hashAll([
      runtimeType,
      value.themeMode,
      value.accentColor,
      value.scaffoldBackgroundColor,
      value.headerTitleColor,
      value.headerBackgroundColor,
      value.bottomBackgroundColor,
      value.selectedItemColor,
      value.unselectedItemColor,
      value.subviewBackgroundColor,
      value.primaryButtonColor,
      value.secondaryButtonColor,
      value.successButtonColor,
      value.warningButtonColor,
      value.dangerButtonColor,
      value.buttonLabelColor,
      value.placeholderColor,
      value.titleTextColor,
      value.subtitleTextColor,
      value.tileBackgroundColor,
      value.avatarBackgroundColor
    ]);
  }
}

extension ColorThemeExtensionBuildContext on BuildContext {
  ColorThemeExtension get colorTheme =>
      Theme.of(this).extension<ColorThemeExtension>()!;
}
